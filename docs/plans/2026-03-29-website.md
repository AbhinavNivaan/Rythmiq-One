# Rythmiq Website Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build and deploy a dark, minimal, single-page marketing website for Rythmiq One at rythmiq.in, targeting investors, accelerators, and B2B partners.

**Architecture:** Plain HTML/CSS/JS — no framework, no build step, no dependencies. Seven sections in a single `index.html`, styles in `css/styles.css`, interactions in `js/main.js`. Contact form handled by Formspree (no backend). Deployed to Firebase Hosting.

**Tech Stack:** HTML5, CSS3, Vanilla JS, Formspree (contact form), Firebase Hosting (Google Cloud), Git

**Spec:** `docs/specs/2026-03-29-website-design.md`

---

## File Map

```
/Users/abhinav/Rythmiq Website/    ← new repo, separate from Rythmiq One
├── index.html                      ← single page, all 7 sections
├── css/
│   └── styles.css                  ← all styles, CSS custom properties for palette
├── js/
│   └── main.js                     ← smooth scroll, mobile nav toggle
├── assets/
│   └── logo.svg                    ← spinner logo as inline SVG (exported once)
├── .gitignore
├── firebase.json                   ← Firebase Hosting config
└── .firebaserc                     ← Firebase project binding
```

---

## Task 1: Project Setup

**Files:**
- Create: `/Users/abhinav/Rythmiq Website/` (new directory)
- Create: `.gitignore`
- Create: `firebase.json`
- Create: `.firebaserc`

- [ ] **Step 1: Create the project directory and initialise git**

```bash
mkdir "/Users/abhinav/Rythmiq Website"
cd "/Users/abhinav/Rythmiq Website"
git init
```

- [ ] **Step 2: Create .gitignore**

```
.DS_Store
.firebase/
```

- [ ] **Step 3: Create firebase.json**

```json
{
  "hosting": {
    "public": ".",
    "ignore": [
      "firebase.json",
      ".firebaserc",
      ".gitignore",
      ".git/**",
      ".firebase/**"
    ],
    "cleanUrls": true,
    "trailingSlash": false
  }
}
```

- [ ] **Step 4: Create .firebaserc**

```json
{
  "projects": {
    "default": "rythmiq-website"
  }
}
```

Note: The Firebase project `rythmiq-website` needs to exist in the Firebase console. If not yet created, go to console.firebase.google.com → Add project → name it `rythmiq-website`. The `.firebaserc` project name must match.

- [ ] **Step 5: Create the directory structure**

```bash
mkdir css js assets
```

- [ ] **Step 6: Initial commit**

```bash
git add .
git commit -m "chore: project setup — firebase hosting config"
```

---

## Task 2: CSS Foundation

**Files:**
- Create: `css/styles.css`

- [ ] **Step 1: Write the base CSS with custom properties and resets**

Create `css/styles.css`:

```css
/* ─── Custom Properties ─────────────────────────────── */
:root {
  --ink: #070712;
  --surface: #0d0f1a;
  --border: #191B26;
  --border-subtle: #2a2d3e;
  --text: #FCFEFF;
  --text-dim: rgba(252, 254, 255, 0.45);
  --text-muted: rgba(252, 254, 255, 0.25);
  --green: #34C759;
  --red: #FF3B30;
  --radius: 10px;
  --max-w: 800px;
}

/* ─── Reset ─────────────────────────────────────────── */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

html { scroll-behavior: smooth; }

body {
  background: var(--ink);
  color: var(--text);
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  font-size: 16px;
  line-height: 1.5;
  -webkit-font-smoothing: antialiased;
}

img, svg { display: block; max-width: 100%; }

a { color: inherit; text-decoration: none; }

/* ─── Utility ────────────────────────────────────────── */
.container {
  max-width: var(--max-w);
  margin: 0 auto;
  padding: 0 24px;
}

.section-label {
  font-size: 11px;
  font-weight: 700;
  letter-spacing: 0.15em;
  text-transform: uppercase;
  margin-bottom: 20px;
}

.section-headline {
  font-size: clamp(26px, 3.5vw, 40px);
  font-weight: 700;
  letter-spacing: -0.02em;
  line-height: 1.15;
  margin-bottom: 40px;
  max-width: 580px;
}

/* ─── Buttons ────────────────────────────────────────── */
.btn-primary {
  display: inline-block;
  padding: 12px 28px;
  background: var(--text);
  color: var(--ink);
  font-size: 15px;
  font-weight: 600;
  border-radius: 8px;
  border: none;
  cursor: pointer;
  font-family: inherit;
  transition: opacity 0.15s;
}
.btn-primary:hover { opacity: 0.88; }

.btn-ghost {
  display: inline-block;
  padding: 12px 20px;
  background: transparent;
  color: var(--text-dim);
  font-size: 14px;
  border: none;
  cursor: pointer;
  font-family: inherit;
}

/* ─── Sections ───────────────────────────────────────── */
section {
  padding: 100px 0;
  border-top: 1px solid var(--border);
}
```

- [ ] **Step 2: Verify the file has no syntax errors**

Open `css/styles.css` in a browser via `index.html` (to be created in Task 3) and check the browser console for CSS errors. Alternatively, paste into https://jigsaw.w3.org/css-validator/ — expect 0 errors.

- [ ] **Step 3: Commit**

```bash
git add css/styles.css
git commit -m "feat: base CSS with custom properties and resets"
```

---

## Task 3: HTML Shell + Nav

**Files:**
- Create: `index.html`
- Modify: `css/styles.css` (append nav styles)

- [ ] **Step 1: Create index.html with the full shell and nav**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Rythmiq One — Your documents, prepared once. Accepted everywhere.">
  <title>Rythmiq</title>
  <link rel="stylesheet" href="css/styles.css">
</head>
<body>

  <!-- NAV -->
  <nav id="nav">
    <div class="nav-inner">
      <a href="/" class="nav-logo" aria-label="Rythmiq home">
        <svg width="28" height="28" viewBox="0 0 56 56" fill="none" aria-hidden="true">
          <rect width="56" height="56" rx="10" fill="#070712"/>
          <rect x="25" y="5" width="6" height="13" rx="3" fill="white"/>
          <rect x="25" y="38" width="6" height="13" rx="3" fill="white" opacity="0.25"/>
          <rect x="38" y="25" width="13" height="6" rx="3" fill="white" opacity="0.65"/>
          <rect x="5" y="25" width="13" height="6" rx="3" fill="white" opacity="0.35"/>
          <rect fill="white" opacity="0.88" rx="3" width="6" height="13" transform="matrix(0.707 0.707 -0.707 0.707 40 10)"/>
          <rect fill="white" opacity="0.18" rx="3" width="6" height="13" transform="matrix(0.707 0.707 -0.707 0.707 15.5 35.5)"/>
          <rect fill="white" opacity="0.50" rx="3" width="6" height="13" transform="matrix(0.707 -0.707 0.707 0.707 31 42)"/>
          <rect fill="white" opacity="0.12" rx="3" width="6" height="13" transform="matrix(0.707 -0.707 0.707 0.707 6 17)"/>
        </svg>
        <span class="nav-wordmark">RYTHMIQ</span>
      </a>
      <a href="#contact" class="btn-primary nav-cta">Book a call</a>
    </div>
  </nav>

  <!-- SECTIONS GO HERE (Tasks 4–9) -->

  <!-- FOOTER -->
  <footer>
    <div class="footer-inner">
      <span>© 2026 Rythmiq. All rights reserved.</span>
      <span>rythmiq.in</span>
    </div>
  </footer>

  <script src="js/main.js"></script>
</body>
</html>
```

- [ ] **Step 2: Append nav and footer styles to css/styles.css**

```css
/* ─── Nav ────────────────────────────────────────────── */
#nav {
  position: sticky;
  top: 0;
  z-index: 100;
  background: rgba(7, 7, 18, 0.85);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border-bottom: 1px solid var(--border);
}
.nav-inner {
  max-width: var(--max-w);
  margin: 0 auto;
  padding: 0 24px;
  height: 56px;
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.nav-logo {
  display: flex;
  align-items: center;
  gap: 10px;
}
.nav-wordmark {
  font-size: 14px;
  font-weight: 700;
  letter-spacing: 0.12em;
}
.nav-cta {
  padding: 7px 18px;
  font-size: 13px;
}

/* ─── Footer ─────────────────────────────────────────── */
footer {
  border-top: 1px solid var(--border);
  padding: 28px 0;
}
.footer-inner {
  max-width: var(--max-w);
  margin: 0 auto;
  padding: 0 24px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-size: 12px;
  color: var(--text-muted);
}
```

- [ ] **Step 3: Open index.html in a browser and verify**

Open `file:///Users/abhinav/Rythmiq\ Website/index.html` in a browser.

Expected: Dark page with sticky nav — logo left, "Book a call" button right. No console errors.

- [ ] **Step 4: Commit**

```bash
git add index.html css/styles.css
git commit -m "feat: HTML shell, nav, and footer"
```

---

## Task 4: Hero Section

**Files:**
- Modify: `index.html` (add hero section)
- Modify: `css/styles.css` (append hero styles)

- [ ] **Step 1: Add the hero section to index.html, replacing the comment**

Replace `<!-- SECTIONS GO HERE (Tasks 4–9) -->` with:

```html
  <!-- 01 HERO -->
  <section id="hero" class="hero">
    <div class="hero-badge">
      <span class="badge-dot" aria-hidden="true"></span>
      <span>Now in development &middot; India</span>
    </div>
    <svg class="hero-logo" width="52" height="52" viewBox="0 0 56 56" fill="none" aria-hidden="true">
      <rect width="56" height="56" rx="10" fill="#0d0f1a"/>
      <rect x="25" y="5" width="6" height="13" rx="3" fill="white"/>
      <rect x="25" y="38" width="6" height="13" rx="3" fill="white" opacity="0.25"/>
      <rect x="38" y="25" width="13" height="6" rx="3" fill="white" opacity="0.65"/>
      <rect x="5" y="25" width="13" height="6" rx="3" fill="white" opacity="0.35"/>
      <rect fill="white" opacity="0.88" rx="3" width="6" height="13" transform="matrix(0.707 0.707 -0.707 0.707 40 10)"/>
      <rect fill="white" opacity="0.18" rx="3" width="6" height="13" transform="matrix(0.707 0.707 -0.707 0.707 15.5 35.5)"/>
      <rect fill="white" opacity="0.50" rx="3" width="6" height="13" transform="matrix(0.707 -0.707 0.707 0.707 31 42)"/>
      <rect fill="white" opacity="0.12" rx="3" width="6" height="13" transform="matrix(0.707 -0.707 0.707 0.707 6 17)"/>
    </svg>
    <h1>Your documents, prepared once.<br>Accepted everywhere.</h1>
    <p class="hero-sub">Rythmiq One auto-prepares your photo, signature, and documents to meet any portal's exact requirements — so you never reformat again.</p>
    <div class="hero-actions">
      <a href="#contact" class="btn-primary">Book a call</a>
      <a href="#problem" class="btn-ghost">Learn more ↓</a>
    </div>
  </section>

  <!-- SECTIONS 02–07 GO HERE -->
```

- [ ] **Step 2: Append hero styles to css/styles.css**

```css
/* ─── Hero ───────────────────────────────────────────── */
.hero {
  min-height: 90vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 80px 24px;
  text-align: center;
  border-top: none;
  position: relative;
  overflow: hidden;
}
.hero::before {
  content: '';
  position: absolute;
  width: 700px;
  height: 700px;
  background: radial-gradient(circle, rgba(252,254,255,0.035) 0%, transparent 70%);
  top: 40%;
  left: 50%;
  transform: translate(-50%, -50%);
  pointer-events: none;
}
.hero-badge {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  border: 1px solid var(--border-subtle);
  border-radius: 100px;
  padding: 4px 12px 4px 8px;
  margin-bottom: 36px;
  background: var(--surface);
  font-size: 11px;
  color: rgba(252,254,255,0.5);
  letter-spacing: 0.05em;
}
.badge-dot {
  width: 6px;
  height: 6px;
  background: var(--green);
  border-radius: 50%;
  flex-shrink: 0;
}
.hero-logo {
  margin-bottom: 28px;
}
.hero h1 {
  font-size: clamp(34px, 5vw, 56px);
  font-weight: 700;
  line-height: 1.1;
  letter-spacing: -0.03em;
  max-width: 680px;
  margin-bottom: 20px;
}
.hero-sub {
  font-size: 17px;
  color: var(--text-dim);
  max-width: 500px;
  line-height: 1.65;
  margin-bottom: 40px;
}
.hero-actions {
  display: flex;
  gap: 12px;
  align-items: center;
  flex-wrap: wrap;
  justify-content: center;
}
```

- [ ] **Step 3: Open in browser and verify**

Expected: Full-screen hero section — badge at top, large logo, headline, sub-headline, two CTAs. "Book a call" should be white button, "Learn more ↓" should be dim ghost text. Subtle radial glow behind the content. No console errors.

- [ ] **Step 4: Commit**

```bash
git add index.html css/styles.css
git commit -m "feat: hero section"
```

---

## Task 5: Problem Section

**Files:**
- Modify: `index.html` (add problem section)
- Modify: `css/styles.css` (append problem styles)

- [ ] **Step 1: Add the problem section to index.html, replacing the comment**

Replace `<!-- SECTIONS 02–07 GO HERE -->` with:

```html
  <!-- 02 PROBLEM -->
  <section id="problem">
    <div class="container">
      <p class="section-label" style="color: var(--red);">The Problem</p>
      <h2 class="section-headline">Every form asks what your document already answers.</h2>
      <div class="pain-grid">
        <div class="pain-card">
          <h3>You never know what you'll get</h3>
          <p>Adjust the DPI, the quality drops. Compress the size, the DPI is off. Convert the format, the compression breaks. Each step undoes the last — and you don't find out until you try to upload.</p>
        </div>
        <div class="pain-card">
          <h3>The tools work against each other</h3>
          <p>There's no single tool that handles size, DPI, format, and quality together. You stitch together three random websites and hope the output is acceptable.</p>
        </div>
        <div class="pain-card">
          <h3>30 seconds or 10 minutes — you won't know</h3>
          <p>That's not a time problem. That's an ambiguity problem. Document prep for a high-stakes form can go either way, and you have no control over which.</p>
        </div>
        <div class="pain-card">
          <h3>Correction windows exist for a reason</h3>
          <p>NEET, JEE, and CAT all build official correction windows into their process because the failure rate is that high. This isn't user error — it's a broken system.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- SECTIONS 03–07 GO HERE -->
```

- [ ] **Step 2: Append problem styles to css/styles.css**

```css
/* ─── Problem ────────────────────────────────────────── */
.pain-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 14px;
}
.pain-card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 24px;
}
.pain-card h3 {
  font-size: 14px;
  font-weight: 600;
  margin-bottom: 10px;
  color: var(--text);
}
.pain-card p {
  font-size: 13px;
  color: var(--text-dim);
  line-height: 1.65;
}
```

- [ ] **Step 3: Open in browser and verify**

Expected: Problem section below hero — red label, large headline, 2×2 grid of pain cards on dark surface. Scroll from hero to problem is smooth. No overflow or alignment issues. No console errors.

- [ ] **Step 4: Commit**

```bash
git add index.html css/styles.css
git commit -m "feat: problem section"
```

---

## Task 6: Solution Section

**Files:**
- Modify: `index.html` (add solution section)
- Modify: `css/styles.css` (append solution styles)

- [ ] **Step 1: Add the solution section to index.html, replacing the comment**

Replace `<!-- SECTIONS 03–07 GO HERE -->` with:

```html
  <!-- 03 SOLUTION -->
  <section id="solution">
    <div class="container">
      <p class="section-label" style="color: var(--green);">The Solution</p>
      <h2 class="section-headline">Prepare once. Let Rythmiq handle the rest.</h2>
      <p class="solution-body">
        You capture your photo, signature, and documents <strong>once</strong> inside the app.
        Rythmiq One enhances them into high-quality masters — then adapts each one to any portal's exact size, format, and DPI requirements, <strong>instantly, on demand.</strong>
      </p>
      <p class="solution-body" style="margin-top: 20px;">
        No reformatting. No unsafe online tools. No doing it again for the next exam.
      </p>
    </div>
  </section>

  <!-- SECTIONS 04–07 GO HERE -->
```

- [ ] **Step 2: Append solution styles to css/styles.css**

```css
/* ─── Solution ───────────────────────────────────────── */
.solution-body {
  font-size: 18px;
  color: var(--text-dim);
  line-height: 1.75;
  max-width: 620px;
}
.solution-body strong {
  color: var(--text);
  font-weight: 600;
}
```

- [ ] **Step 3: Open in browser and verify**

Expected: Solution section — green label, headline, two paragraphs with bolded key phrases in white. Clean, readable. No console errors.

- [ ] **Step 4: Commit**

```bash
git add index.html css/styles.css
git commit -m "feat: solution section"
```

---

## Task 7: How It Works Section

**Files:**
- Modify: `index.html` (add how it works section)
- Modify: `css/styles.css` (append steps styles)

- [ ] **Step 1: Add the how it works section to index.html**

Replace `<!-- SECTIONS 04–07 GO HERE -->` with:

```html
  <!-- 04 HOW IT WORKS -->
  <section id="how-it-works">
    <div class="container">
      <p class="section-label" style="color: var(--text-muted);">How It Works</p>
      <div class="steps">
        <div class="step">
          <p class="step-num">01 — CAPTURE</p>
          <h3>Once.</h3>
          <p>Take a photo, scan a document, or sign on screen. That's the last time you'll need to.</p>
        </div>
        <div class="step">
          <p class="step-num">02 — ENHANCE</p>
          <h3>Perfected automatically.</h3>
          <p>Rythmiq auto-corrects, crops, and optimises everything into a high-quality master stored in your vault.</p>
        </div>
        <div class="step">
          <p class="step-num">03 — EXPORT</p>
          <h3>Any portal. Instantly.</h3>
          <p>Select your exam. Get a file that meets its exact specs — right size, right format, right DPI.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- SECTIONS 05–07 GO HERE -->
```

- [ ] **Step 2: Append steps styles to css/styles.css**

```css
/* ─── How It Works ───────────────────────────────────── */
.steps {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: 1px;
  background: var(--border);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  overflow: hidden;
}
.step {
  background: var(--surface);
  padding: 28px 24px;
}
.step-num {
  font-size: 11px;
  font-weight: 700;
  letter-spacing: 0.12em;
  color: var(--green);
  margin-bottom: 14px;
}
.step h3 {
  font-size: 16px;
  font-weight: 600;
  margin-bottom: 10px;
  color: var(--text);
}
.step p {
  font-size: 13px;
  color: var(--text-dim);
  line-height: 1.65;
}
```

- [ ] **Step 3: Open in browser and verify**

Expected: Three-column steps block with 1px grid separators between cells — looks like a connected table. Green step numbers, white step headings, dim body text. No console errors.

- [ ] **Step 4: Commit**

```bash
git add index.html css/styles.css
git commit -m "feat: how it works section"
```

---

## Task 8: Vision Section

**Files:**
- Modify: `index.html` (add vision section)
- Modify: `css/styles.css` (append vision styles)

- [ ] **Step 1: Add the vision section to index.html**

Replace `<!-- SECTIONS 05–07 GO HERE -->` with:

```html
  <!-- 05 VISION -->
  <section id="vision">
    <div class="container">
      <p class="section-label" style="color: var(--text-muted);">The Vision</p>
      <h2 class="section-headline">Where we're going.</h2>
      <p class="vision-body">
        Documents are just the beginning.
      </p>
      <p class="vision-body">
        Every form you fill — NEET, JEE, CAT, government applications, corporate onboarding — asks for information your documents already contain. Name, date of birth, address, ID numbers. You type it in, every single time.
      </p>
      <p class="vision-body">
        The vision for Rythmiq is <strong>one tap.</strong> Open a form, and it's already filled — pulled directly from your verified documents, formatted to the portal's exact requirements. No typing. No uploading. No reformatting. Just submit.
      </p>
      <blockquote class="vision-pull">
        We're building the layer between every Indian student and every form they'll ever fill.
      </blockquote>
    </div>
  </section>

  <!-- SECTIONS 06–07 GO HERE -->
```

- [ ] **Step 2: Append vision styles to css/styles.css**

```css
/* ─── Vision ─────────────────────────────────────────── */
.vision-body {
  font-size: 18px;
  color: var(--text-dim);
  line-height: 1.75;
  max-width: 620px;
  margin-bottom: 20px;
}
.vision-body strong {
  color: var(--text);
  font-weight: 600;
}
.vision-pull {
  margin-top: 40px;
  padding: 28px 32px;
  background: var(--surface);
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius);
  font-size: 20px;
  font-weight: 600;
  line-height: 1.4;
  letter-spacing: -0.01em;
  color: var(--text);
  max-width: 560px;
}
```

- [ ] **Step 3: Open in browser and verify**

Expected: Vision section with muted label, large headline, three body paragraphs with "one tap" bolded, and a pull quote in a dark card. No console errors.

- [ ] **Step 4: Commit**

```bash
git add index.html css/styles.css
git commit -m "feat: vision section"
```

---

## Task 9: Founder Section

**Files:**
- Modify: `index.html` (add founder section)
- Modify: `css/styles.css` (append founder styles)

- [ ] **Step 1: Add the founder section to index.html**

Replace `<!-- SECTIONS 06–07 GO HERE -->` with:

```html
  <!-- 06 FOUNDER -->
  <section id="founder">
    <div class="container">
      <p class="section-label" style="color: var(--text-muted);">Founder</p>
      <div class="founder-card">
        <div class="founder-avatar" aria-label="Abhinav Prakash">AP</div>
        <div class="founder-content">
          <p class="founder-name">Abhinav Prakash</p>
          <p class="founder-title">Founder, Rythmiq</p>
          <p class="founder-bio">
            I'm a student. I've filled out exam forms for myself and for every member of my family when they needed it.
          </p>
          <p class="founder-bio">
            I know exactly what it feels like to spend time reformatting the same documents, juggling random online tools, hoping the portal accepts the upload before the deadline closes.
          </p>
          <p class="founder-bio">
            I built Rythmiq because this problem is too common and too solvable to still exist.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- SECTION 07 GO HERE -->
```

- [ ] **Step 2: Append founder styles to css/styles.css**

```css
/* ─── Founder ────────────────────────────────────────── */
.founder-card {
  display: flex;
  gap: 32px;
  align-items: flex-start;
}
.founder-avatar {
  width: 64px;
  height: 64px;
  border-radius: 50%;
  background: var(--surface);
  border: 1px solid var(--border-subtle);
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 18px;
  font-weight: 700;
  color: var(--text-muted);
  letter-spacing: 0.05em;
}
.founder-name {
  font-size: 18px;
  font-weight: 600;
  margin-bottom: 4px;
}
.founder-title {
  font-size: 12px;
  color: var(--text-muted);
  letter-spacing: 0.08em;
  text-transform: uppercase;
  margin-bottom: 20px;
}
.founder-bio {
  font-size: 15px;
  color: var(--text-dim);
  line-height: 1.7;
  max-width: 560px;
  margin-bottom: 14px;
}
.founder-bio:last-child { margin-bottom: 0; }
```

Note: When Abhinav provides a photo, replace the `.founder-avatar` div with an `<img>` tag:
```html
<img src="assets/founder.jpg" alt="Abhinav Prakash" class="founder-avatar" style="object-fit:cover;">
```

- [ ] **Step 3: Open in browser and verify**

Expected: Founder section — "AP" avatar circle on the left, name, title, and three bio paragraphs on the right. No console errors.

- [ ] **Step 4: Commit**

```bash
git add index.html css/styles.css
git commit -m "feat: founder section"
```

---

## Task 10: Contact Section

**Files:**
- Modify: `index.html` (add contact section)
- Modify: `css/styles.css` (append contact styles)

- [ ] **Step 1: Sign up for Formspree**

1. Go to https://formspree.io and create a free account using founder@rythmiq.in
2. Create a new form → set submission email to `founder@rythmiq.in`
3. Copy the form endpoint — it looks like `https://formspree.io/f/xyzabcde`
4. Replace `YOUR_FORMSPREE_ENDPOINT` in the HTML below with that URL

- [ ] **Step 2: Add the contact section to index.html**

Replace `<!-- SECTION 07 GO HERE -->` with:

```html
  <!-- 07 CONTACT -->
  <section id="contact">
    <div class="container">
      <p class="section-label" style="color: var(--green);">Get in Touch</p>
      <h2 class="contact-headline">Let's talk.</h2>
      <p class="contact-sub">Investor, accelerator, or potential partner — reach out below.</p>
      <form
        id="contact-form"
        action="YOUR_FORMSPREE_ENDPOINT"
        method="POST"
      >
        <input type="text" name="name" placeholder="Your name" required autocomplete="name">
        <input type="email" name="email" placeholder="Your email" required autocomplete="email">
        <textarea name="message" placeholder="Your message" required rows="5"></textarea>
        <button type="submit" class="btn-primary">Send message</button>
        <p id="form-status" class="form-status" aria-live="polite"></p>
      </form>
      <p class="contact-email">
        Or email directly:
        <a href="mailto:founder@rythmiq.in">founder@rythmiq.in</a>
      </p>
    </div>
  </section>
```

- [ ] **Step 3: Append contact styles to css/styles.css**

```css
/* ─── Contact ────────────────────────────────────────── */
.contact-headline {
  font-size: clamp(26px, 3.5vw, 36px);
  font-weight: 700;
  letter-spacing: -0.02em;
  margin-bottom: 12px;
}
.contact-sub {
  font-size: 15px;
  color: var(--text-dim);
  margin-bottom: 36px;
}
#contact-form {
  display: flex;
  flex-direction: column;
  gap: 14px;
  max-width: 480px;
}
#contact-form input,
#contact-form textarea {
  background: var(--surface);
  border: 1px solid var(--border-subtle);
  border-radius: 8px;
  padding: 12px 16px;
  font-size: 14px;
  color: var(--text);
  font-family: inherit;
  outline: none;
  width: 100%;
  transition: border-color 0.15s;
}
#contact-form input:focus,
#contact-form textarea:focus {
  border-color: rgba(252,254,255,0.3);
}
#contact-form input::placeholder,
#contact-form textarea::placeholder {
  color: var(--text-muted);
}
#contact-form textarea { resize: vertical; }
#contact-form .btn-primary { align-self: flex-start; }
.form-status {
  font-size: 13px;
  color: var(--green);
  min-height: 20px;
}
.form-status.error { color: var(--red); }
.contact-email {
  margin-top: 20px;
  font-size: 14px;
  color: var(--text-muted);
}
.contact-email a {
  color: var(--text-dim);
  border-bottom: 1px solid var(--border-subtle);
  transition: color 0.15s;
}
.contact-email a:hover { color: var(--text); }
```

- [ ] **Step 4: Open in browser and verify**

Expected: Contact section — green label, "Let's talk." headline, sub-headline, form with three fields and submit button, email link below. No console errors.

- [ ] **Step 5: Commit**

```bash
git add index.html css/styles.css
git commit -m "feat: contact section with Formspree form"
```

---

## Task 11: JavaScript — Smooth Scroll + Form Feedback

**Files:**
- Create: `js/main.js`

- [ ] **Step 1: Create js/main.js**

```js
// Smooth scroll for all anchor links (supplement html { scroll-behavior: smooth })
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
  anchor.addEventListener('click', function (e) {
    const target = document.querySelector(this.getAttribute('href'));
    if (!target) return;
    e.preventDefault();
    target.scrollIntoView({ behavior: 'smooth', block: 'start' });
  });
});

// Contact form — AJAX submission with status feedback
const form = document.getElementById('contact-form');
const status = document.getElementById('form-status');

if (form) {
  form.addEventListener('submit', async function (e) {
    e.preventDefault();
    const btn = form.querySelector('button[type="submit"]');
    btn.disabled = true;
    btn.textContent = 'Sending...';
    status.textContent = '';
    status.className = 'form-status';

    try {
      const response = await fetch(form.action, {
        method: 'POST',
        body: new FormData(form),
        headers: { Accept: 'application/json' }
      });

      if (response.ok) {
        status.textContent = "Message sent. I'll be in touch soon.";
        form.reset();
      } else {
        status.textContent = 'Something went wrong. Email me directly at founder@rythmiq.in';
        status.classList.add('error');
      }
    } catch {
      status.textContent = 'Something went wrong. Email me directly at founder@rythmiq.in';
      status.classList.add('error');
    } finally {
      btn.disabled = false;
      btn.textContent = 'Send message';
    }
  });
}
```

- [ ] **Step 2: Verify smooth scroll**

Open `index.html` in browser. Click "Book a call" in the nav. Expected: page scrolls smoothly to the contact section. Click "Learn more ↓" in hero. Expected: scrolls to problem section.

- [ ] **Step 3: Verify form feedback (without a real Formspree endpoint)**

Temporarily change the form action to a bad URL (e.g. `https://invalid.example`). Submit the form. Expected: error message appears in green/red status area, button re-enables.

Restore the real Formspree endpoint after testing.

- [ ] **Step 4: Commit**

```bash
git add js/main.js
git commit -m "feat: smooth scroll and AJAX form submission with feedback"
```

---

## Task 12: Mobile Responsiveness

**Files:**
- Modify: `css/styles.css` (append responsive styles)

- [ ] **Step 1: Append responsive styles to css/styles.css**

```css
/* ─── Responsive ─────────────────────────────────────── */
@media (max-width: 640px) {
  section { padding: 72px 0; }

  .hero { padding: 72px 24px; }
  .hero h1 { font-size: 32px; }
  .hero-sub { font-size: 15px; }

  .pain-grid { grid-template-columns: 1fr; }

  .steps { grid-template-columns: 1fr; }
  .step { border-top: 1px solid var(--border); }
  .step:first-child { border-top: none; }

  .founder-card { flex-direction: column; gap: 20px; }

  .footer-inner { flex-direction: column; gap: 8px; text-align: center; }

  .nav-inner { padding: 0 16px; }
  .container { padding: 0 16px; }
}
```

- [ ] **Step 2: Verify on mobile viewport**

In Chrome DevTools (F12 → mobile icon), set viewport to 390×844 (iPhone 14).

Check each section:
- Nav: logo and button both visible, no overflow
- Hero: headline readable, both buttons stack cleanly
- Problem: cards stack to single column
- How it works: steps stack vertically
- Founder: avatar above text, not side by side
- Contact: form full width
- Footer: items stack centered

- [ ] **Step 3: Commit**

```bash
git add css/styles.css
git commit -m "feat: mobile responsive styles"
```

---

## Task 13: Deploy to Firebase Hosting

**Files:**
- No code changes

- [ ] **Step 1: Install Firebase CLI if not already installed**

```bash
npm install -g firebase-tools
```

Expected: `firebase --version` prints a version number (e.g. `13.x.x`).

- [ ] **Step 2: Log in to Firebase**

```bash
firebase login
```

Expected: Browser opens for Google login. Log in with the Google account that owns the Firebase project.

- [ ] **Step 3: Verify the Firebase project exists**

```bash
firebase projects:list
```

Expected: `rythmiq-website` appears in the list. If it doesn't exist, create it:
1. Go to https://console.firebase.google.com
2. Add project → name: `rythmiq-website`
3. Disable Google Analytics (not needed)
4. Create project

- [ ] **Step 4: Deploy**

```bash
cd "/Users/abhinav/Rythmiq Website"
firebase deploy --only hosting
```

Expected output ends with:
```
✔  Deploy complete!
Hosting URL: https://rythmiq-website.web.app
```

- [ ] **Step 5: Open the deployed URL and do a final smoke test**

Open `https://rythmiq-website.web.app` in a browser.

Check:
- [ ] Page loads over HTTPS
- [ ] Nav is sticky on scroll
- [ ] All 7 sections present and correctly styled
- [ ] "Book a call" scrolls to contact
- [ ] "Learn more ↓" scrolls to problem
- [ ] Contact form submits and shows success message
- [ ] Page looks correct on mobile (use DevTools)

- [ ] **Step 6: Connect the custom domain rythmiq.in**

In Firebase console → Hosting → Add custom domain → enter `rythmiq.in`.

Firebase will provide DNS records (A records or CNAME). Add them in your domain registrar's DNS settings.

Wait for propagation (up to 24h). Firebase provisions SSL automatically.

- [ ] **Step 7: Final commit**

```bash
git add .
git commit -m "chore: confirm firebase deployment and custom domain setup"
```

---

## Self-Review

**Spec coverage check:**

| Spec requirement | Task |
|---|---|
| Dark & Premium visual direction | Task 2 (CSS custom properties) |
| Sticky frosted nav — logo + Book a call | Task 3 |
| Hero — tagline, sub-headline, badge, CTA | Task 4 |
| Problem — headline + 4 pain cards | Task 5 |
| Solution — headline + body | Task 6 |
| How it works — 3 steps | Task 7 |
| Vision — body + pull quote | Task 8 |
| Founder — AP initials, name, bio | Task 9 |
| Contact — form (name/email/message) + email | Task 10 |
| "Book a call" scrolls to contact | Task 11 (smooth scroll) |
| Formspree for form backend | Task 10 |
| Firebase Hosting | Task 13 |
| Custom domain rythmiq.in | Task 13, Step 6 |
| Mobile responsiveness | Task 12 |
| founder@rythmiq.in public email | Task 10 |

All requirements covered. No gaps.
